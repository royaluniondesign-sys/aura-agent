# Handoff — Sesión 2026-06-07
*Para la próxima sesión Claude Code en `~/aura/`*

## Estado al cerrar

### Servicios vivos
- AURA bot (@rudagency_bot) → PID activo, polling Telegram OK
- Hermes (@rudserverbot) → OpenClaw puerto 18789, conectado
- Termora → puerto 4030
- ComfyUI → puerto 8188
- Voice → puerto 8085

### Git
- PR abierto: https://github.com/royaluniondesign-sys/aura-agent/pull/2
- Rama: `chore/aios-reorganization-2026-06-07`
- **Pendiente**: merge del PR + volver a `main`

---

## Siguiente sesión: AURA + Hermes actualizan Obsidian

### Objetivo
Que AURA y Hermes sean responsables de mantener el vault Obsidian actualizado automáticamente — sin intervención manual.

### Qué implementar

**1. Tool AURA: `obsidian_update`**
- Archivo: `~/aura/src/actions/tools/obsidian_tool.py`
- Funciones:
  - `obsidian_write(file, content)` — escribe/actualiza una nota
  - `obsidian_append(file, content)` — añade al final
  - `obsidian_daily_log(text)` — añade al daily note de hoy en `04-daily/`
  - `obsidian_commit()` — hace git commit en `~/.aura/memory/`
- Pattern: `@aura_tool` — se auto-registra en MCP y Telegram

**2. Loop de sync Obsidian en AURA (proactive_loop)**
- Cada 15 min AURA ya corre `proactive_loop`
- Añadir rutina: actualizar `AURA_Dashboard.md` con estado real de servicios
- Fuente: `launchctl list`, puertos TCP, logs recientes
- Archivo a tocar: `src/infra/proactive_loop.py`

**3. Hermes → inbox Obsidian**
- Cuando Hermes completa una tarea importante → escribe en `~/.aura/memory/00-inbox/`
- Via MCP server AURA: tool `obsidian_write` ya expuesta
- Configurar en: `~/.openclaw/workspace/TOOLS.md` o via cron en OpenClaw

**4. Git auto-commit en vault**
- obsidian-git ya hace commit local cada 10 min ✅
- Siguiente: añadir remote GitHub para backup real
  ```bash
  cd ~/.aura/memory
  gh repo create royaluniondesign-sys/aura-memory --private
  git remote add origin <url>
  git push -u origin main
  # Luego activar autoPushInterval: 60 en obsidian-git data.json
  ```

### Contexto a leer al iniciar
```bash
cat ~/aura/CLAUDE.md                          # reglas y estructura
cat ~/aura/docs/architecture/SYSTEM_MAP.md    # mapa del sistema
cat ~/aura/docs/runbooks/INCIDENCIAS.md       # errores conocidos
cat ~/aura/docs/phases/ROADMAP.md             # qué falta
cat ~/.aura/memory/MEMORY.md                  # estado owner
```

### Verificación rápida del sistema
```bash
launchctl list | grep -E "aura|hermes|termora"
python3 -c "
import socket
for port, name in [(3002,'AURA'), (8085,'Voice'), (18789,'Hermes'), (4030,'Termora'), (8188,'ComfyUI')]:
    try:
        socket.create_connection(('127.0.0.1', port), timeout=1)
        print(f'✅ {name}:{port}')
    except: print(f'❌ {name}:{port}')
"
```

### PR pendiente de merge
```bash
cd ~/aura
gh pr merge 2 --squash
git checkout main && git pull
```
